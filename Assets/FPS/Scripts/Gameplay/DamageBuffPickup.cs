using System.Collections;
using System.Collections.Generic;
using Unity.FPS.Game;
using UnityEngine;

public class DamageBuffPickup : MonoBehaviour
{
    //public float BuffMultiplier = 2f;
    //public float BuffDuration = 10f;

    //private void OnTriggerEnter(Collider other)
    //{
    //    WeaponController weapon = other.GetComponentInChildren<WeaponController>();
    //    if (weapon != null)
    //    {
    //        weapon.ApplyDamageBuff(BuffMultiplier, BuffDuration);
    //        Destroy(gameObject); // remove the buff pickup
    //    }
    //}

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
